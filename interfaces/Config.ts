export type ImportConfig = {
  url: string
}

export type GenerateConfig = {
  size: number
  k: number
  mode: string
}

export type ExportConfig = {
  filename: string
  type: string
  quality: number
}
